# ============================================
# MT5Services - 更新版依赖清单
# ============================================

# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.1

# MT5 连接
MetaTrader5>=5.0.45

# 数据库
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
alembic>=1.13.0  # 数据库迁移

# 数据处理（必需）
pandas>=2.0.0
numpy>=1.24.0

# 技术分析指标
ta-lib>=0.4.28  # 标准技术指标库

# 缓存（推荐）
redis>=5.0.0

# 监控与指标
prometheus-client>=0.19.0

# 异步支持
asyncio>=3.4.3
aiofiles>=23.2.0

# 配置管理
pyyaml>=6.0.0
watchfiles>=0.21.0  # 配置热重载

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # 异步HTTP客户端测试

# 开发工具
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0

# 日志改进
structlog>=23.2.0

# 安全增强
python-jose[cryptography]>=3.3.0  # JWT认证
passlib[bcrypt]>=1.7.4  # 密码哈希

# 时间处理
pytz>=2023.3
python-dateutil>=2.8.2

# 网络工具
requests>=2.31.0
websockets>=12.0  # WebSocket支持

# 性能优化
orjson>=3.9.0  # 最快的JSON库

# ============================================
# 可选依赖（按需安装）
# ============================================

# 机器学习（可选）
scikit-learn>=1.3.0

# 图表（可选）
plotly>=5.17.0
matplotlib>=3.8.0

# 部署优化（可选）
gunicorn>=21.2.0
uvloop>=0.19.0

# ============================================
# 环境特定依赖组
# ============================================

[dev]
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0
flake8>=6.1.0
jupyter>=1.0.0
ipython>=8.17.0

[test]
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
factory-boy>=3.3.0
faker>=20.0.0

[prod]
gunicorn>=21.2.0
uvloop>=0.19.0
sentry-sdk>=1.38.0  # 错误监控