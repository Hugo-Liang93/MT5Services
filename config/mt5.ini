[mt5]
# One service instance binds to one active account for its full lifetime.
# Select that startup account here.
default_account = default
login =
password =
server =
path =
timezone = UTC

[account.demo]
# Extra profiles are for startup selection or multi-instance deployment.
# They are not switchable at runtime through the API.
enabled = false
label = Demo
login =
password =
server =
path =
timezone = UTC
