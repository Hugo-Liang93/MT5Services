# risk.ini — live-exec-b 可选账户级风控覆盖
#
# 非敏感、可提交版本库的账户差异可写在这里；
# 敏感或本机差异请写到同目录 risk.local.ini。

[risk]
# max_trades_per_day =
# max_trades_per_hour =
# daily_loss_limit_pct =

[margin_guard]
# enabled =
# warn_level =
# danger_level =
# critical_level =
