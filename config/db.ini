# db.ini — TimescaleDB（PostgreSQL）连接配置
#
# 服务启动时自动初始化 schema 和 hypertable（若不存在）。
# 需要预先在 PostgreSQL 中安装 TimescaleDB 扩展。
# 生产环境凭据建议写入 config/db.local.ini（已被 .gitignore 忽略）。

[db]
# 数据库主机地址
host = localhost

# 数据库端口（PostgreSQL 默认 5432）
port = 5432

# 数据库用户名
user = postgres

# 数据库密码
password = postgres

# 数据库名称
database = mt5

# Schema 名称（通常为 public）
schema = public
