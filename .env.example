# Deprecated
#
# Runtime configuration is loaded from:
# - config/*.ini
# - config/indicators.json
# - optional config/*.local.ini local overrides
#
# This project no longer reads `.env` as a runtime config source.
# Keep secrets in `config/*.local.ini`, which is already ignored by Git.
