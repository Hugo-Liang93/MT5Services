# ============================================
# MT5Services - 增强版依赖清单
# ============================================

# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.1

# MT5 连接
MetaTrader5>=5.0.45

# 数据库
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
alembic>=1.13.0  # 数据库迁移
asyncpg>=0.29.0  # 异步PostgreSQL驱动

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0  # 科学计算
ta-lib>=0.4.28  # 技术分析库

# 缓存
redis>=5.0.0
aioredis>=2.0.0  # 异步Redis

# 监控与指标
prometheus-client>=0.19.0
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.42b0

# 消息队列（可选）
celery>=5.3.0
redis>=5.0.0  # Celery broker

# 异步任务
asyncio>=3.4.3
aiofiles>=23.2.0
aiohttp>=3.9.0

# 配置管理
pyyaml>=6.0.0
watchfiles>=0.21.0  # 配置热重载

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # 异步HTTP客户端测试

# 开发工具
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0
flake8>=6.1.0
pre-commit>=3.5.0

# 日志
structlog>=23.2.0
python-json-logger>=2.0.7

# 安全
python-jose[cryptography]>=3.3.0  # JWT认证
passlib[bcrypt]>=1.7.4  # 密码哈希
cryptography>=41.0.0

# 时间处理
pytz>=2023.3
python-dateutil>=2.8.2

# 网络工具
requests>=2.31.0
websockets>=12.0  # WebSocket支持

# 性能优化
ujson>=5.8.0  # 更快的JSON解析
orjson>=3.9.0  # 最快的JSON库
msgpack>=1.0.7  # 二进制序列化

# 机器学习（可选）
scikit-learn>=1.3.0
tensorflow>=2.13.0  # 或 pytorch>=2.0.0
joblib>=1.3.0

# 文档生成
mkdocs>=1.5.0
mkdocs-material>=9.4.0
mkdocstrings[python]>=0.24.0

# ============================================
# 可选依赖组（按需安装）
# ============================================

# 回测框架（可选）
backtrader>=1.9.78.123
vectorbt>=0.25.0

# 高级图表（可选）
plotly>=5.17.0
matplotlib>=3.8.0
seaborn>=0.13.0

# 数据源（可选）
yfinance>=0.2.33  # Yahoo Finance
ccxt>=4.1.0  # 加密货币交易所
alpha-vantage>=3.0.0  # Alpha Vantage API

# 部署相关（可选）
gunicorn>=21.2.0
uvloop>=0.19.0  # 更快的asyncio事件循环
httptools>=0.6.0  # HTTP解析优化

# ============================================
# 环境特定依赖
# ============================================

# 开发环境
[dev]
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0
flake8>=6.1.0
pre-commit>=3.5.0
jupyter>=1.0.0
ipython>=8.17.0

# 测试环境
[test]
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-xdist>=3.5.0
factory-boy>=3.3.0
faker>=20.0.0

# 生产环境
[prod]
gunicorn>=21.2.0
uvloop>=0.19.0
httptools>=0.6.0
sentry-sdk>=1.38.0  # 错误监控
newrelic>=8.10.0.174  # APM监控