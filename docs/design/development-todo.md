# 开发待办

## 待决事项

- `signal.ini` 的交易成本阈值仍偏紧：当前 `base_spread_points = 30`、`max_spread_to_stop_ratio = 0.33` 会持续触发启动警告，并可能在低 ATR 时段大量拦截自动交易。这个属于交易策略参数决策，暂未自动修改。
- 需要明确“非 `signal.ini` 配置热重载”的开发策略：目前缓存可以刷新，但运行中的市场、经济日历、监控等组件是否应该原地热应用，还是统一要求重建运行时，仍需要项目层决策。
- `market.ini` 中依赖 FastAPI 应用壳层的配置项（如 `docs_enabled`、`redoc_enabled`、CORS 中间件装配）仍是“应用创建时生效”。如果后续要求这类配置也支持热重载，需要决定是重建整个 `FastAPI app`，还是明确规定此类变更必须重启服务。
