# 2026-04-19 H1 Baseline（P7 delta 修复后真实值）

> **⚠️ 时点快照**：记录 **2026-04-19 当日** P7 delta 指标注入修复后首次与生产行为对齐的 H1 baseline。
> **不回改正文**。后续 baseline 重跑写新文件（`docs/research/<日期>-*.md`）。
>
> 触发：上一 session commit `84dd9b8`（PR #48）修复 `_required_indicators` 聚合丢失 `htf_requirements`，
> 本 session 修复 `breakout_follow` 对 `adx_d3=None` 硬拒 bug —— 至此 delta 指标在回测中真正生效，
> 消除"delta 失效下假象膨胀"的 473 笔 baseline。
>
> **数据窗口**：2025-04-17 ~ 2026-04-15（12 个月），XAUUSD，全策略
>
> 相关 commit：`84dd9b8` / `42c37bb` / `eae0ab8` / `63454b2`（Merge PR #48）

---

## H1 整体 baseline 演进

| 维度 | 原 baseline | P5 bugfix 后 | **P7 delta 修复后（当前真实值）** |
|------|---------|---------|---------|
| Trades | 201 | 473 | **346** |
| WR | 44.3% | 46.1% | **46.2%** |
| PF | 2.595 | 2.361 | **2.041** |
| Sharpe | 2.168 | 2.74 | **2.508** |
| MaxDD | 11.69% | 8.0% | **6.93%** |
| MaxDD duration | — | 75 bars | **61 bars** |

## H1 策略分布（P7 修复后真实）

| 策略 | n | WR | PnL（$10k→$44k compound）| 备注 |
|------|---|-----|------|------|
| strong_trend_follow | 45 | **55.6%** | +$16,494 | `adx_d3_min_strict > 0` 门控生效 |
| regime_exhaustion | 15 | **80.0%** | +$13,947 | `adx_d3 > 0` 门控选出真耗竭 |
| breakout_follow | 115 | 43.5% | +$2,233 | |
| open_range_breakout | 72 | 38.9% | +$745 | 不依赖 delta |
| trendline_touch | 79 | 46.8% | +$414 | 不依赖 delta |
| pullback_window | 20 | 40.0% | +$323 | `rsi_d3` 门控生效 |

**重要**：P5 bugfix 后的 473 笔 baseline 是 **delta 门控失效下的假象膨胀**。P7 修复后 346 笔是**可投产参考**。旧 baseline 数据（201 笔、473 笔）均不应作为未来参数调优 / 策略评估的参考。

## P7 修复的量化影响

- Trades 473 → **346** (-27%)：门控生效消除虚假交易
- Sharpe 2.74 → 2.508：合理下降（更真实）
- MaxDD 8.0% → **6.93%**：风险更低
- **regime_exhaustion WR 57.7% → 80.0%**（+22.3pp）：`adx_d3 > 0` 门控选出真耗竭反转
- **strong_trend_follow WR 50% → 55.6%**：`adx_d3_min_strict > 0` 选出真趋势
- open_range_breakout / trendline_touch 不依赖 delta → 无变化（修复无 side effect）

## P7 修复思路

**根因**：不是 delta metric 未实现（`src/indicators/runtime/delta_metrics.py` 已有），而是回测管线 `BacktestEngine._pipeline.compute()` 绕过了 `UnifiedIndicatorManager.query_services.apply_delta_metrics_query`（该路径依赖 MarketService 历史）。

**解决方案**：在 `src/backtesting/engine/indicators.py::precompute_all_indicators` 末尾新增 snapshot-based delta 注入（`_apply_delta_to_snapshots`），数据源为 snapshots 自身，不依赖 MarketService，字段命名 / 精度与生产完全一致。

## `signal.local.ini` 本机 override（不入 git，当期生效）

```ini
[regime_affinity.structured_trend_continuation]
trending = 0.0
ranging = 0.0
breakout = 0.0
uncertain = 0.0
```

冻结原因：solo min_conf=0.10 时 33 trades PF 1.22；solo min_conf=0.45 时 **16 trades PF 0.61** —— 高 raw_confidence 反而 WR 更低，置信度校准反向。保留代码资产，未来可重设计后解冻。

## 未决跟进（写入 TODO.md）

- M5/M30/M15 等其他 TF baseline 重跑（当前只对 H1 做了对比）→ 2026-04-20 已完成 M30/H4，见 [2026-04-20-tf-baseline-review.md](2026-04-20-tf-baseline-review.md)
- P5 残留的 breakout_follow 参数调优需基于新 baseline 重新评估
- FP.2 `strong_trend_follow` Paper 观察数据可能偏离当前回测值（Paper 数据在 bugfix 前记录）

## 挖掘驱动策略状态（补记）

- `regime_exhaustion`（已部署 paper_only）— **新 baseline 主要盈利贡献**：52 trades / 57.7% WR / +$47,391（compound 后）。**建议 Paper 观察独立跟踪**。
- `strong_trend_follow`（FP.2，已部署 paper_only）— 新 baseline 次主要贡献：62 trades / 50% WR / +$25,549。
